<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.zenoss.zep</groupId>
    <artifactId>zep-parent</artifactId>
    <packaging>pom</packaging>
    <version>2.0.0</version>
    <name>Zenoss Event Processor Parent</name>
    <url>http://www.zenoss.com</url>
    <modules>
        <module>core</module>
        <module>webapp</module>
        <module>dist</module>
    </modules>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- This database is used for integration tests (dropped/added each time). -->
        <zep.test.jdbc.dbname>zenoss_zep_test</zep.test.jdbc.dbname>
        <zep.test.index.dir>${project.build.directory}/index</zep.test.index.dir>
        <zep.test.amqp.uri>amqp://zenoss:zenoss@localhost:5672/zenoss-test</zep.test.amqp.uri>
        <!-- Dependency Versions -->
        <version.jackson>1.9.2</version.jackson>
        <version.jetty>7.5.3.v20111011</version.jetty>
        <version.logback>0.9.26</version.logback>
        <version.lucene>4.7.2</version.lucene>
        <version.metrics>2.2.0</version.metrics>
        <version.mysql>5.1.15</version.mysql>
        <version.postgresql>9.0-801.jdbc4</version.postgresql>
        <version.resteasy>2.0.1.GA</version.resteasy>
        <version.slf4j>1.6.1</version.slf4j>
        <version.spring>3.0.5.RELEASE</version.spring>
        <version.tomcat-jdbc>7.0.27</version.tomcat-jdbc>
        <zenoss-utils.version>2.0.0</zenoss-utils.version>
        <zenoss-protocols.version>2.0.2</zenoss-protocols.version>
        <metric-consumer.version>0.0.11</metric-consumer.version>
    </properties>
    <profiles>
        <!-- Disable the integration tests if we haven't specified a database type -->
        <profile>
            <id>integration-tests</id>
            <activation>
                <property>
                    <name>!dbtype</name>
                </property>
            </activation>
            <properties>
                <skipITs>true</skipITs>
            </properties>
        </profile>
        <profile>
            <id>mysql</id>
            <activation>
                <property>
                    <name>dbtype</name>
                    <value>mysql</value>
                </property>
            </activation>
            <properties>
                <zep.jdbc.protocol>${dbtype}</zep.jdbc.protocol>
                <zep.jdbc.hostname>localhost</zep.jdbc.hostname>
                <zep.jdbc.port>3306</zep.jdbc.port>
                <zep.jdbc.admin.username>root</zep.jdbc.admin.username>
                <zep.jdbc.admin.password></zep.jdbc.admin.password>
                <zep.jdbc.username>zenoss</zep.jdbc.username>
                <zep.jdbc.password>zenoss</zep.jdbc.password>
            </properties>
        </profile>
        <profile>
            <id>postgresql</id>
            <activation>
                <property>
                    <name>dbtype</name>
                    <value>postgresql</value>
                </property>
            </activation>
            <properties>
                <zep.jdbc.protocol>${dbtype}</zep.jdbc.protocol>
                <zep.jdbc.hostname>localhost</zep.jdbc.hostname>
                <zep.jdbc.port>5432</zep.jdbc.port>
                <zep.jdbc.admin.username>postgres</zep.jdbc.admin.username>
                <zep.jdbc.admin.password>postgres</zep.jdbc.admin.password>
                <zep.jdbc.username>zenoss</zep.jdbc.username>
                <zep.jdbc.password>zenoss</zep.jdbc.password>
            </properties>
        </profile>
    </profiles>
    <scm>
        <connection>scm:svn:http://dev.zenoss.com/svnint/trunk/core/zep</connection>
        <developerConnection>scm:svn:http://dev.zenoss.com/svnint/trunk/core/zep</developerConnection>
        <url>http://dev.zenoss.com/tracint/browser/trunk/core/zep</url>
    </scm>
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.8.2</version>
            <type>jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.easymock</groupId>
            <artifactId>easymock</artifactId>
            <version>3.0</version>
            <type>jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>findbugs-maven-plugin</artifactId>
                    <version>2.5.2</version>
                    <configuration>
                        <xmlOutput>true</xmlOutput>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>1.7</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <detectJavaApiLink>true</detectJavaApiLink>
                    <excludePackageNames>*.impl.*,*.rest.*</excludePackageNames>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>1.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.8</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>2.1.2</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
